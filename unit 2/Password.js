var isValid = false;
while(!isValid) { //at the starting point this means not false, but it changes when isValid is true
  var password = prompt("What is your password?");
  if (password == "secret"){
    isValid = true;
  }
}

// var isInvalid = true;
// while(isInvalid) { //at the starting point this means not false, but it changes when isValid is true
//   var password = prompt("What is your password?");
//   if (password == "secret"){
//     isInvalid = false;
//   }
// }



// continuess

//we break the loop when the condition in the while loop is false
